using UnityEngine;

public class FingerTipCollider : MonoBehaviour
{
    // This script currently does nothing except exist as an identifier.
    // It lets objects know this collider is a fingertip when we build grabbing later.
}
